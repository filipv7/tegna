document.getElementById('year').textContent = new Date().getFullYear();
const PRICE_PER_M2 = 100;const areaEl=document.getElementById('area');const want3dEl=document.getElementById('want3d');const totalEl=document.getElementById('total');const calcBtn=document.getElementById('calcBtn');const toContactBtn=document.getElementById('toContact');function formatNOK(n){return new Intl.NumberFormat('no-NO',{style:'currency',currency:'NOK',maximumFractionDigits:0}).format(n)}function calc(){const area=Math.max(0,Number(areaEl.value||0));const base=area*PRICE_PER_M2;totalEl.textContent=formatNOK(base)+(want3dEl.checked?' + 3D-tillegg':'')}if(calcBtn)calcBtn.addEventListener('click',calc);if(areaEl)areaEl.addEventListener('input',calc);if(want3dEl)want3dEl.addEventListener('change',calc);if(toContactBtn)toContactBtn.addEventListener('click',()=>{const areal=Number(areaEl.value||0);const arealInput=document.querySelector('form.contact-form input[name="areal"]');if(arealInput&&areal>0)arealInput.value=areal;document.getElementById('kontakt').scrollIntoView({behavior:'smooth'})});